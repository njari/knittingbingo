# Frontend
# VITE_API_URL should point to your deployed API Gateway base URL (include trailing slash)
VITE_API_URL=https://<api-id>.execute-api.<region>.amazonaws.com/prod/

# Backend / CDK (these are for your CI / GitHub Actions as secrets)
# Add these as GitHub repository secrets (do NOT check them into git)
# AWS_ACCESS_KEY_ID=...
# AWS_SECRET_ACCESS_KEY=...
# AWS_REGION=us-east-1
