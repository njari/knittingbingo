# Frontend
# VITE_API_URL should point to your deployed Lambda Function URL (include trailing slash)

# AWS creds (GitHub Actions secrets) used by backend CFN + frontend S3 deploy
# Add these as GitHub repository secrets (do NOT check them into git)
# AWS_ACCESS_KEY_ID=...
# AWS_SECRET_ACCESS_KEY=...
# AWS_REGION=us-east-1

# Frontend deploy bucket
# S3 bucket to host the built frontend:
#   bingoknitting

# Backend artifact bucket
# S3 bucket to store Lambda zip for CloudFormation:
#   bingoknitting-artifacts
 