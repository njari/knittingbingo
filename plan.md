Today, we are making a simple knitting bingo application. I want a web based frontend made with React and a lambda based backend with dynamodb as the backing database. 

This is a multi user app. Please add a feature to use a magic link delivered via email. 
The magic link has an authorization code that is forever valid(as of now). Save it as a user against the key for 
